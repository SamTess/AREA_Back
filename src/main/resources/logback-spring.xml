<configuration>

    <!-- Only log ERROR and above by default -->
    <property name="LOG_LEVEL" value="DEBUG" />

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <!-- Console appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%-5level %logger{36} - %msg%n%n</pattern>
            <immediateFlush>true</immediateFlush>
        </encoder>
    </appender>

    <!-- Reduce noise -->
    <logger name="org.springframework" level="ERROR"/>
    <logger name="org.springframework.web" level="ERROR"/>
    <logger name="org.springframework.security" level="ERROR"/>
    <logger name="org.hibernate" level="ERROR"/>
    <logger name="org.hibernate.SQL" level="ERROR"/>
    <logger name="org.hibernate.type" level="ERROR"/>
    <logger name="i.l.core" level="WARN"/>
    <logger name="io.lettuce.core" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="o.quartz.core" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="ERROR"/>

    <!-- Application package logger - enable DEBUG for our services -->
    <logger name="area.server" level="DEBUG"/>

    <root level="${LOG_LEVEL:-ERROR}">
        <appender-ref ref="CONSOLE" />
    </root>
</configuration>
