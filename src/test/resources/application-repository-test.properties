# Test Configuration specifically for Repository tests
spring.application.name=AREA_Back-Test

# Test Database Configuration (H2 in-memory with PostgreSQL compatibility mode)
spring.datasource.url=jdbc:h2:mem:testdb;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH;INIT=CREATE SCHEMA IF NOT EXISTS area
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA Configuration for Tests
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect
spring.jpa.properties.hibernate.hbm2ddl.create_namespaces=true
spring.jpa.properties.hibernate.default_schema=area

# Disable Flyway for tests
spring.flyway.enabled=false

# Disable Redis for tests
spring.data.redis.repositories.enabled=false

# Disable Quartz scheduler for tests
spring.quartz.auto-startup=false

# Logging Configuration for Tests
logging.level.area.server.AREA_Back=DEBUG
logging.level.org.springframework.web=WARN
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE